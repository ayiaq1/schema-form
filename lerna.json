{
  "version": "0.3.1",
  "packages": ["packages/*"],
  "command": {
    "run": {
      "npmClient": "npm"
    },
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"]
    },
    "version": {
      "allowBranch": ["release/*"]
    }
  }
}
